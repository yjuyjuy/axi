# Single source of truth for the AXI catalog.
#
# To add your AXI: append an entry below, run `pnpm run docs:gen`, and commit
# this file together with the regenerated README.md and docs/index.html.
# CI (`pnpm run docs:check`) fails if the rendered tables drift from this file.
#
# Descriptions support `inline code` and [markdown links](https://example.com);
# keep them to one sentence or two short clauses.

official:
  - name: gh-axi
    url: https://github.com/kunchenguid/gh-axi
    domain: GitHub
    description: "Issues, PRs, workflow runs, releases, and more. Wraps the official `gh` CLI with agent-ergonomic output."
  - name: chrome-devtools-axi
    url: https://github.com/kunchenguid/chrome-devtools-axi
    domain: Browser automation
    description: "Navigate, click, fill, and extract with combined operations and query filtering. Wraps chrome-devtools-mcp."
  - name: lavish-axi
    url: https://github.com/kunchenguid/lavish-axi
    domain: Human review
    description: "Turns agent-generated HTML artifacts into collaborative review surfaces - annotate, comment, and send feedback back to the agent."
  - name: quota-axi
    url: https://github.com/kunchenguid/quota-axi
    domain: Quota / usage
    description: "Reports local Claude, Codex, Cursor, Copilot, and Grok quota/usage windows for routing-aware agents - data-only and local-first."

community:
  - name: jj-axi
    url: https://github.com/aivv73/jj-axi
    author: aivv73
    domain: Version control
    description: "Inspect and reshape Jujutsu history through deterministic, non-interactive commands with compact TOON output and operation-aware undo."
  - name: npm-axi
    url: https://github.com/SSBrouhard/npm-axi
    author: SSBrouhard
    domain: npm
    description: "Search and inspect npm registry packages, versions, dependencies, README previews, and downloads with token-efficient output."
  - name: sqlite-axi
    url: https://github.com/SSBrouhard/sqlite-axi
    author: SSBrouhard
    domain: SQLite
    description: "Inspect schemas, sample rows, and run capped read-only SQLite queries with token-efficient TOON output."
  - name: slack-axi
    url: https://github.com/JarvusInnovations/slack-axi
    author: Jarvus Innovations
    domain: Slack
    description: "Read, search, sweep, and safely draft Slack messages with token-efficient output."
  - name: gws-axi
    url: https://github.com/JarvusInnovations/gws-axi
    author: Jarvus Innovations
    domain: Google Workspace
    description: "Gmail, Calendar, Docs, Drive, and Slides behind one command, with multi-account write-safety - drafts mail, never sends."
  - name: harvest-axi
    url: https://github.com/JarvusInnovations/harvest-axi
    author: Jarvus Innovations
    domain: Time tracking
    description: "Review, log, and edit Harvest time entries by period - for yourself, your team, a project, or a client."
  - name: specops
    url: https://github.com/JarvusInnovations/specops
    author: Jarvus Innovations
    domain: Spec-driven dev
    description: "Spec-driven development for agents - and a demo of shipping an AXI embedded in a skill, not a standalone npm executable."
  - name: gitsheets-axi
    url: https://github.com/JarvusInnovations/gitsheets/tree/main/packages/gitsheets-axi
    author: Jarvus Innovations
    domain: Git-backed data
    description: "Read and mutate git-backed record sheets over the shell - TOON output, idempotent commits."
  - name: metabase-axi
    url: https://github.com/JarvusInnovations/metabase-axi
    author: Jarvus Innovations
    domain: Analytics / BI
    description: "Query, explore, and export from Metabase over the shell - SQL/MBQL, saved questions, schema introspection, full-data export."
  - name: otter-axi
    url: https://github.com/JarvusInnovations/otter-axi
    author: Jarvus Innovations
    domain: Meetings
    description: "Find and pull Otter.ai meeting transcripts from the shell - wraps Otter.ai's hosted MCP server as a scriptable, headless CLI."
  - name: notion-axi
    url: https://github.com/maximebrmd/notion-axi
    author: maximebrmd
    domain: Notion
    description: "Search, read, create, and update Notion pages and databases over the shell - token-efficient TOON output, PAT or integration auth."
  - name: clickup-axi
    url: https://github.com/JanSuthacheeva/clickup-axi
    author: JanSuthacheeva
    domain: ClickUp
    description: "List your open tasks, view a task with its newest comments inline, and change task status - truncation-aware, TOON-style output."
  - name: databricks-axi
    url: https://github.com/p33ves/databricks-axi
    author: p33ves
    domain: Databricks
    description: "Run Databricks jobs, watch runs, and pull failure logs inline over the official databricks CLI - more domains coming soon."
  - name: aws-axi
    url: https://github.com/thatdudealso/aws-axi
    author: thatdudealso
    domain: AWS
    description: "Discover, plan, provision, deploy, and inspect AWS services for hosting web, backend, database, and AI workloads through safe token-efficient CLI workflows."
  - name: docker-axi
    url: https://github.com/thatdudealso/docker-axi
    author: thatdudealso
    domain: Docker
    description: "Discover, build, run, debug, publish, inspect, and clean up Docker apps through safe token-efficient CLI workflows."
  - name: doctl-axi
    url: https://github.com/batamire/doctl-axi
    author: batamire
    domain: DigitalOcean
    description: "Agent-ergonomic CLI for DigitalOcean — one AXI wrapping doctl → TOON, replacing 21 MCPs."
  - name: dynamodb-axi
    url: https://github.com/thatdudealso/dynamodb-axi
    author: thatdudealso
    domain: DynamoDB
    description: "Discover, inspect, query, scan, create, update, back up, restore, export, import, and safely operate DynamoDB tables through token-efficient CLI workflows."
  - name: pg-axi
    url: https://github.com/thatdudealso/pg-axi
    author: thatdudealso
    domain: PostgreSQL
    description: "Discover, create, inspect, query, back up, restore, and maintain PostgreSQL databases through safe token-efficient CLI workflows."
  - name: mongodb-axi
    url: https://github.com/thatdudealso/mongodb-axi
    author: thatdudealso
    domain: MongoDB
    description: "Discover, create, inspect, query, export, import, maintain, and diagnose MongoDB databases through safe token-efficient CLI workflows."
  - name: elasticsearch-axi
    url: https://github.com/thatdudealso/elasticsearch-axi
    author: thatdudealso
    domain: Elasticsearch
    description: "Discover, inspect, query, index, map, snapshot, restore, diagnose, and operate Elasticsearch clusters through safe token-efficient CLI workflows."
  - name: kubernetes-axi
    url: https://github.com/thatdudealso/kubernetes-axi
    author: thatdudealso
    domain: Kubernetes
    description: "Discover, inspect, deploy, debug, scale, roll out, expose, and clean up Kubernetes workloads through safe token-efficient CLI workflows."
  - name: redis-axi
    url: https://github.com/thatdudealso/redis-axi
    author: thatdudealso
    domain: Redis
    description: "Discover, inspect, query, export, import, maintain, and diagnose Redis databases through safe token-efficient CLI workflows."
  - name: celery-axi
    url: https://github.com/thatdudealso/celery-axi
    author: thatdudealso
    domain: Celery
    description: "Discover, inspect, run, debug, monitor, schedule, control, and safely operate Celery task queues through token-efficient CLI workflows."
  - name: cyber-mux
    url: https://github.com/cyberuni/cyber-mux
    author: unional
    domain: Terminal multiplexers
    description: "Open, send, read, focus, and close terminal panes across tmux, herdr, and WezTerm through one detection-driven contract with token-efficient output."
  - name: oracle-axi
    url: https://github.com/thatdudealso/oracle-axi
    author: thatdudealso
    domain: Oracle Database
    description: "Discover, create, inspect, query, export, import, maintain, and diagnose Oracle databases through safe token-efficient CLI workflows."
  - name: glab-axi
    url: https://github.com/karotkriss/glab-axi
    author: karotkriss
    domain: GitLab
    description: "Issues, merge requests, CI/CD pipelines, variables and secrets, releases, and raw API access. Wraps the official glab CLI with agent-ergonomic TOON output."
  - name: reactive-axi
    url: https://github.com/adeeshsharma/reactive-axi
    author: adeeshsharma
    domain: UI review
    description: "Click any element in your live React, Vue, or Svelte dev server and resolve it to the exact source file and line before it reaches your agent."
  - name: obsidian-axi
    url: https://github.com/AndersHoffmann/obsidian-axi
    author: AndersHoffmann
    domain: Obsidian
    description: "Read, search, create, update, organize, and link Obsidian notes from the filesystem - compact TOON output, atomic writes, no plugin or server."
  - name: comfy-cloud-axi
    url: https://github.com/intelligentrascal/comfy-cloud-axi
    author: intelligentrascal
    domain: AI generation
    description: "Generate images, video, and audio via 22 partner models, submit ComfyUI workflows and templates, track jobs, estimate credits before generating, search nodes, and report usage — wraps the Comfy Cloud MCP server with token-efficient TOON output."
    admission:
      status: exception
      reviewed_revision: 0f5b75eb201512604c87f6d75f4234bb685e0879
      exception: "The published 0.1.6 package reports version 0.1.4; `generate ... --confirm --type` defaults to image and `job chain <id> --dry-run` passes NaN before MCP calls; nodes and saved workflows report empty results for `{}` responses; paid `workflow run` and `templates run` ignore surplus positionals; and other mutating commands can ignore trailing flags."
  - name: mobbin-axi
    url: https://github.com/intelligentrascal/mobbin-axi
    author: intelligentrascal
    domain: UI/UX research
    description: "Search real-world UI/UX patterns from production apps via Mobbin - wraps Mobbin's OAuth-gated MCP server as a scriptable, headless CLI with token-efficient TOON output."
    admission:
      status: exception
      reviewed_revision: 8eda65af7d760efe45907ad3368d3164abe3062e
      exception: "The published 0.1.3 package reports version 0.1.1; search-command --help is rejected; empty-content MCP responses can become successful `0 results`; sections ignores --platform; control commands lack per-command flag sets; and legacy --full and --json flags are no-ops."
  - name: calendly-axi
    url: https://github.com/JarvusInnovations/calendly-axi
    author: Jarvus Innovations
    domain: Scheduling
    description: "The Calendly booking loop from the shell - see what's scheduled, mint single-use booking links, book directly, cancel, and manage event types, availability, and webhooks."
  - name: remarkable-axi
    url: https://github.com/JarvusInnovations/remarkable-axi
    author: Jarvus Innovations
    domain: E-ink tablets
    description: "Manage content on your reMarkable tablet through the reMarkable cloud - browse and organize files, upload PDFs and EPUBs, and send web articles as reflowable EPUBs."
  - name: ado-axi
    url: https://github.com/dtabolich/ado-axi
    author: dtabolich
    domain: Azure DevOps
    description: "Pull requests, branches, repositories, and pipeline runs. Wraps Azure CLI (`az devops` / `az repos`) with agent-ergonomic TOON output."
  - name: ado-axi
    url: https://github.com/jeffreyhaen/ado-axi
    author: jeffreyhaen
    domain: Azure DevOps
    description: "Work items, pull requests, Git refs, pipelines, and raw Azure DevOps REST calls with safe mutations, profiles, and token-efficient TOON output."
    admission:
      status: admitted
      reviewed_revision: 5b997626c17a7798f8aea4ff31613e97886a3cc1
      reviewed_components:
        - src/bin/ado-axi.ts
        - src/lib/args.ts
        - src/lib/format.ts
        - src/commands/home.ts
        - src/commands/workItem.ts
        - src/commands/pr.ts
        - src/commands/pipeline.ts
        - src/commands/ref.ts
        - src/commands/api.ts
        - src/commands/config.ts
        - src/lib/client.ts
      source_observations:
        - "src/commands/api.ts accepts an optional positional HTTP verb, validates path, host, query, body, content type, and raw-response options before calling the shared request client."
        - "src/commands/config.ts lists configured profiles and initializes named profiles with organization, optional project, and az or PAT authentication settings."
        - "src/lib/client.ts constructs encoded Azure DevOps request URLs, resolves credentials, and translates network and HTTP failures into AxiError responses."
  - name: supabase-axi
    url: https://github.com/laizhenyoong/supabase-axi
    author: laizhenyoong
    domain: Supabase
    description: "Inspect schemas, run SQL, and audit RLS, indexes, and logs across Supabase projects. Wraps the Supabase Management API."
  - name: homebrew-axi
    url: https://github.com/mstuart/homebrew-axi
    author: mstuart
    domain: Homebrew
    description: "Inspect Homebrew formulae, casks, and installed packages — versions, dependencies, outdated status, and install stats — with token-efficient TOON output; read-only, never mutates."
  - name: pypi-axi
    url: https://github.com/mstuart/pypi-axi
    author: mstuart
    domain: PyPI
    description: "Inspect PyPI packages, versions, dependencies, and download stats with token-efficient TOON output — read-only, no auth required."
  - name: cargo-axi
    url: https://github.com/AG9898/cargo-axi
    author: AG9898
    domain: Rust / Cargo
    description: "Inspect Rust workspaces and run safe `check` or Clippy validation with compact TOON output and opt-in session context."
  - name: forgejo-axi
    url: https://github.com/escidmore/forgejo-axi
    author: escidmore
    domain: Forgejo
    description: "Pull request lifecycle, issues, and raw API access for self-hosted Forgejo - non-interactive, reconciling mutations, with token-efficient TOON output."
  - name: mastra-axi
    url: https://github.com/taltas/mastra-axi
    author: taltas
    domain: Mastra
    description: "Discover and run a Mastra app's agents, tools, and workflows through agent-native shell commands with compact TOON output."
  - name: axi-axi
    url: https://github.com/CodyEngel/axi-axi
    author: CodyEngel
    domain: AXI authoring
    description: "Scaffold a compliant AXI, read the 10 principles in token-tuned slices, and run read-only compliance checks against a candidate CLI."
  - name: jira-axi
    url: https://github.com/emilchristensen/atlassian-axi/tree/main/packages/jira-axi
    author: emilchristensen
    domain: Issue tracking
    description: "Create, search, transition, and comment on Jira work items, boards, and sprints through Atlassian's acli with token-efficient TOON output and idempotent mutations."
  - name: confluence-axi
    url: https://github.com/emilchristensen/atlassian-axi/tree/main/packages/confluence-axi
    author: emilchristensen
    domain: Documentation
    description: "Read and write Confluence Cloud pages and spaces directly over the REST API with token-efficient TOON output and OAuth 3LO or API-token auth."
  - name: az-axi
    url: https://github.com/masyanru/az-axi
    author: masyanru
    domain: Azure
    description: "Discover, inspect, query, and safely mutate Azure through token-efficient CLI workflows over every az module."
  - name: mssql-axi
    url: https://github.com/jeffreyhaen/mssql-axi
    author: jeffreyhaen
    domain: Microsoft SQL Server / Azure SQL
    description: "Inspect schemas, sample rows, run capped read-only queries and showplans over the native ODBC driver - mutations double-gated with dry-run by default, TOON output."
  # Admission review (independent source review per VISION.md): inspected
  # Holaxis-ai/superbee at release v0.1.3, commit
  # f4e1c37349627030f8201ff52028f71a9c92570a. Direct source observations:
  # packages/cli/AXI-CONTRACT.md pins the agent-facing interaction contract;
  # src/output.ts renders TOON by default with a compact --json escape hatch;
  # src/errors.ts maps typed failures to stable error codes on the capped
  # 0/1/2/4/5/6 exit taxonomy with structured error envelopes; src/commands/
  # list.ts bounds repeated rows and reports count/shown with a --limit 0
  # escape; src/commands/doc/common.ts gates doc update/delete behind
  # --expected-version compare-and-swap and keeps delete idempotent; and
  # src/commands/home.ts serves a zero-arg live orientation view. Verdict:
  # the source-defined interface and behavior at that revision satisfy the
  # applicable AXI principles; admitted.
  - name: superbee
    url: https://github.com/Holaxis-ai/superbee
    author: Holaxis
    domain: Agent knowledge infrastructure
    description: "Turns what agents know into durable, domain-shaped infrastructure through local OKF bundles with declared schemas, exact queries, conflict-safe writes, and portable Views."
  - name: figma-axi
    url: https://github.com/ardaatahan/figma-axi
    author: ardaatahan
    domain: Design / Figma
    description: "Inspect Figma files, export nodes to PNG/SVG/PDF, and read styles, components, and comments - summarizes huge files instead of dumping raw JSON."
  - name: trello-axi
    url: https://github.com/enzodevs/trello-axi
    author: enzodevs
    domain: Project management
    description: "Create, search, update, move, comment on, and archive Trello cards through the REST API with compact TOON output, complete pagination, idempotent mutations, and non-interactive batch workflows."
  - name: railway-axi
    url: https://github.com/simkimsia/railway-axi
    author: simkimsia
    domain: Railway
    description: "List projects and inspect linked project status, environments, and services. Wraps the Railway CLI with agent-ergonomic TOON output and structured errors."
  - name: netlify-axi
    url: https://github.com/simkimsia/netlify-axi
    author: simkimsia
    domain: Netlify
    description: "List sites and inspect the linked site's status and the logged-in account. Wraps the Netlify CLI with agent-ergonomic TOON output and structured errors."
  # Admission review (independent source review per VISION.md): inspected
  # ardaatahan/vercel-axi at commit
  # 81c88e3582c3018fca8fcc1c4db40c993ece6f18 (no tagged release exists).
  # Direct source observations: src/commands/vercel.ts gates production
  # deploys, promote/rollback, domain add/remove, dns add/remove, and env
  # add/remove behind requireConfirm(), which throws UsageError absent
  # --confirm; src/output/errors.ts maps UsageError to exit 2 and AxiError
  # to exit 1 on the documented 0/1/2 taxonomy; src/vercel/process.ts wraps
  # the official `vercel` binary via spawnSync, converts a missing binary
  # or non-zero exit into a structured AxiError, and detects auth failures
  # to point at `vercel login`/VERCEL_TOKEN; env list redacts value/secret/
  # decryptedValue fields by default (vercel.ts redact()); src/index.ts
  # registers only these gated commands, with no ungated destructive path.
  # Verdict: the source-defined interface and behavior at that revision
  # satisfy the applicable AXI principles; admitted.
  - name: vercel-axi
    url: https://github.com/ardaatahan/vercel-axi
    author: ardaatahan
    domain: Deployment / Vercel
    description: "Deploy and inspect Vercel deployments, projects, domains, DNS, environment variables, and teams through the official Vercel CLI, with explicit confirmation gates for production and infrastructure changes."
  - name: cloudflare-axi
    url: https://github.com/simkimsia/cloudflare-axi
    author: simkimsia
    domain: Cloudflare
    description: "Inspect the current Worker's deployments, Pages projects, KV namespaces, and the logged-in account. Wraps the Cloudflare CLI (wrangler) with agent-ergonomic TOON output and structured errors."
  - name: coolify-axi
    url: https://github.com/radityasurya/coolify-axi
    author: radityasurya
    domain: Coolify
    description: "Apps, databases, services, and deployments on self-hosted or cloud Coolify - name-based addressing, redacted secrets, and TOON output over the official coolify CLI."
    admission:
      status: exception
      reviewed_revision: 513e9bb2d9addf35135c9fcdd8d7f57ce0de4a43
      reviewed_components:
        - bin/coolify-axi.js
        - src/cli.js
        - src/args.js
        - src/coolify.js
        - src/commands/app.js
        - src/commands/db.js
      source_observations:
        - "src/cli.js passes a `home` handler to runAxiCli that fetches `resource list` and returns counts, a state rollup, and per-resource state, so a no-argument run emits live data rather than the static TOON help in TOP_HELP."
        - "src/args.js parses each subcommand with node:util parseArgs in strict mode and converts ERR_PARSE_ARGS_UNKNOWN_OPTION into an AxiError with code VALIDATION_ERROR whose suggestion names that subcommand's valid flags."
        - "src/coolify.js runs the `coolify` binary through execFile and translates failures into AxiError codes MISSING_DEPENDENCY (ENOENT), AUTH_ERROR, NOT_FOUND, and COOLIFY_ERROR after dropping the wrapped CLI's update banner from stderr; redact()/redactValue() replace secret-named fields and the password segment matched by URL_CREDENTIALS in connection URLs unless --reveal is passed; resolveResource() raises VALIDATION_ERROR listing candidate uuids when a name matches more than one resource."
        - "src/commands/app.js projects `app get` through the DETAIL_FIELDS allow-list, truncates `app logs` to the last LOG_LIMIT (4000) characters with a size hint and a --full escape hatch, and returns `unchanged: true` without calling the wrapped CLI when start or stop targets a resource already in that state."
      exception: "`db get <name|uuid>` in src/commands/db.js returns a normal result payload reading `no database named <name>` for a lookup miss, so the process exits 0; the equivalent miss for applications, services, and servers goes through resolveResource() in src/coolify.js and raises a NOT_FOUND AxiError."
  - name: porkbun-axi
    url: https://github.com/ardaatahan/porkbun-axi
    author: ardaatahan
    domain: Domains / Porkbun
    description: "Manage Porkbun domains, DNS records, SSL certificates, URL forwarding, nameservers, and glue records through the Porkbun API, with compact TOON output and explicit confirmation gates for registrations and destructive updates/deletes."
    admission:
      status: exception
      reviewed_revision: 394fb206ea58c6d0aaa33a0f24bd6f70f84f206e
      reviewed_components:
        - src/commands/porkbun.ts
        - src/output/errors.ts
        - src/api.ts
        - src/cli/args.ts
        - src/index.ts
      source_observations:
        - "src/commands/porkbun.ts defines gate() which prints the change then throws UsageError unless --confirm is set; dns update/delete, forwarding delete, nameservers set, and glue update/delete call gate(); domains register uses its own --confirm check (not gate()), preflights checkDomain, then submits cost in cents with agreeToTerms only after --confirm; dns update refuses to run without --name."
        - "src/output/errors.ts maps UsageError to exit 2 and AxiError to exit 1 on the documented 0/1/2 taxonomy; src/cli/args.ts throws UsageError on unknown flags with the valid flag set for that command; src/index.ts registers only allCommands from porkbun.ts."
        - "src/api.ts reads PORKBUN_API_KEY and PORKBUN_SECRET_KEY from the environment into X-API-Key / X-Secret-API-Key headers (never the request body) and attaches a fresh Idempotency-Key UUID on idempotent writes; src/commands/porkbun.ts ssl retrieve prints PEM component sizes by default and only emits the certificate material with --json."
      exception: "`dns create`, `forwarding create`, and `glue create` in src/commands/porkbun.ts print a change line then POST the mutation; they do not take --confirm or call gate()."
  - name: wrangler-axi
    url: https://github.com/masculinecache/wrangler-axi
    author: masculinecache
    domain: Cloudflare Wrangler
    description: "Deploy Workers and Pages, manage KV namespaces, D1 databases, R2 buckets, and Worker secrets, and tail live logs with hard bounds - wraps the official Wrangler CLI with predictable TOON lists and structured errors."
    admission:
      status: exception
      reviewed_revision: 67ff6c45e12f4bb71e42ee8b71d586480c0861be
      reviewed_components:
        - src/cli.ts
        - src/commands/workers.ts
        - src/args.ts
        - src/util.ts
        - src/wrangler.ts
        - src/errors.ts
        - bin/wrangler-axi.ts
      source_observations:
        - "src/cli.ts registers whoami, workers, secrets, kv, d1, r2, and pages areas and returns structured output plus exit codes without calling process.exit; src/commands/workers.ts bounds tail streams by --max-entries or --timeout and renders compact event summaries."
        - "src/args.ts and src/util.ts validate flags and convert unknown areas, subcommands, and flags into AxiError VALIDATION_ERROR responses with corrective suggestions; src/wrangler.ts maps non-zero Wrangler results into structured errors and reports a missing Wrangler dependency explicitly."
      exception: "Mutating commands do not provide an AXI-level --confirm gate; delete and similar operations only forward Wrangler's --force behavior as -f or -y."
  - name: chezmoi-axi
    url: https://github.com/masculinecache/chezmoi-axi
    author: masculinecache
    domain: Dotfiles
    description: "List, diff, add, re-add, apply, verify, and sync chezmoi-managed dotfiles with compact TOON output - template-aware re-add verdicts, preview dry-runs, and idempotent operations."
    admission:
      status: exception
      reviewed_revision: ba1c969e1c92471c2fdfc82a6bfad70361fcf2d1
      reviewed_components:
        - chezmoi-axi
      source_observations:
        - "chezmoi-axi implements status, list, diff, add, re-add, apply, verify, sync, and commit as a single shell entrypoint with compact structured output, command-specific help, and exit 0/1/2 handling for success, runtime errors, and usage errors."
        - "re-add uses chezmoi's verbose dry-run to distinguish changed, template-managed, and in-sync files; list caps output at 50 rows with --full, and sync adds branch protection, bounded log rotation, and a preview mode for headless operation."
      exception: "apply and sync mutate the chezmoi state without an AXI-level --confirm gate; --preview provides a dry-run escape hatch."
  - name: mem-axi
    url: https://github.com/masculinecache/axi-memory
    author: masculinecache
    domain: Agent memory
    description: "Durable cross-session agent memory as markdown + YAML frontmatter in a git repo - typed memories, priority-ranked search with L0 abstracts, OS-side dedup, and cross-machine sync."
    admission:
      status: admitted
      reviewed_revision: f6fd3d37079540efb168a189b7a81573b472fb31
      reviewed_components:
        - mem
        - docs/design.md
        - docs/integration.md
        - docs/harnesses.md
      source_observations:
        - "mem stores typed markdown memories with YAML frontmatter under objects/<type>, supports init, add, update, merge, dedup, search, show, list, review, status, sync, and stats, and emits compact structured output with explicit empty states and exit 0/1/2 errors."
        - "mem add refuses credential-like title or body content unless --force is supplied, while dedup is dry-run by default and search/list rank results by priority and freshness with --limit and --min-priority controls; sync bounds pull and push operations with a 30-second timeout."

  - name: mattermost-axi
    # TODO: replace with the final URL once the repo lands on the company GitLab (tooling/mattermost-axi, migration ticket 19).
    url: https://github.com/yjuyjuy/mattermost-axi
    author: yjuyjuy
    domain: Mattermost
    description: "Read threads and attachments, search posts, and reply in threads with token-efficient TOON output and --json contracts; credentials from MM_TOKEN and MM_SERVER_URL only."
